# 2026-04-08

Summary: Two-layer logs in repo and vault, with the dev log kept human-readable

> Historical note: the first update below came from the legacy single-file dev log. New edits should stay short and update this same file.

## Updates

### 2026-04-08 | Two-layer log architecture

- changed: `logs/DEV_LOG.md` is now navigation only and the real dev notes live in `logs/dev/days/`
- why: the old single-file dev log was readable only until the project speed picked up
- vault: mirrored the same dev-log shape into `projects/pathfinder/sources/docs/DEV_LOG.md` plus `projects/pathfinder/sources/docs/dev-log/days/`
- next: future dev-log updates on the same day go into this file, then `python scripts\\manage_dev_log.py rebuild`

### 2026-04-08 | Logging review pass

- changed: reviewed the new log flow and fixed the real break points instead of assuming the first migration was enough
- found: some live PathFinder docs still told agents to append straight into `DEV_LOG.md`, and the scripts had no safe `ensure-day` or `validate` step
- fixed: patched the live vault source docs, added `ensure-day` and `validate` to `manage_dev_log.py`, and removed the mistaken repo general-log lane completely
- next: new dev-log days should start with `python scripts\\manage_dev_log.py ensure-day --date YYYY-MM-DD --summary \"...\"`, then rebuild and validate

### Entry 032 - 2026-04-08

**Goal:** Stop the mirrored dev log from growing as one unbounded live file and make rotation safe instead of manual.

**Decision:** Added `scripts/manage_dev_log.py` as the explicit paired dev-log maintenance tool. `logs/DEV_LOG.md` remains the live repo mirror head, the vault copy remains canonical, and archive rotation now happens by date cutoff into paired archive files instead of hand-editing the live logs. The tool refuses archive runs if the repo and vault live copies differ after normalizing BOM and CRLF noise.

**What changed:** Added `scripts/manage_dev_log.py` and `test_manage_dev_log.py`, updated `logs/README.md`, updated the vault repo/vault sync policy note, and archived all entries before `2026-04-01` into `D:\ANHDUC\Path_finder\logs\archive\DEV_LOG.before-2026-04-01.md` plus `D:\ANHDUC\ADUC_vault\ADUC\projects\pathfinder\sources\docs\archive\dev-log\DEV_LOG.before-2026-04-01.md`.

**Verification:**
- `python test_manage_dev_log.py`
- `python scripts\manage_dev_log.py status`
- `python scripts\manage_dev_log.py archive --before 2026-04-01`
- `python scripts\manage_dev_log.py status`

**What I learned:** Log growth is not a note-writing problem. It needs a hard operational boundary: one live head file, explicit archive cutoffs, and a sync guard that fails closed before mutating either side.

### 2026-04-08 | Anchor-stage routing and prompt ownership

- changed: added `anchor_stage` / `anchor_mode` semantics so `current_stage` stays funnel logic while the live turn owner can temporarily move for revisit or forced detours
- fixed: stage routing now follows `anchor_stage`, B1 prompt assembly now anchors reasoning and `PROBE` on the active stage, and contradict / rebound prompts become optional pivot offers instead of silent stage hijacks
- added: stage extractors now receive a re-entry signal so reopened stages treat locked fields as under review instead of inheriting confidence blindly
- bucket: orchestrator token optimization is now the main production blocker for release hardening and should be treated as the next production bucket item
