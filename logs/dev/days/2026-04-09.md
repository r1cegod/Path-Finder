# 2026-04-09

Summary: Confidence prompts aligned to `> 0.8`, replayed cleanly, and structured-output serializer warning debt paid

## Updates

### 2026-04-09 | Knowledge-agent confidence lock audit

- changed: aligned `backend/data/prompts/thinking.py`, `backend/data/prompts/purpose.py`, and `backend/data/prompts/goals.py` with Python's shared `> 0.8` done threshold so `0.7-0.8` is explicitly provisional instead of lock-safe
- fixed: extractor prompts now say `done` is Python-owned schema state, reserve `> 0.8` for fields that survive reopening, and protect already locked fields at `> 0.8` rather than `> 0.7`
- verified:
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/thinking_attack_v2.jsonl --graph thinking_eval --workers 3`
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/thinking_attack.jsonl --graph thinking_eval --workers 1`
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/purpose_attack.jsonl --graph purpose_eval --workers 4`
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/goals_attack.jsonl --graph goals_eval --workers 4`
- found: all replay runs completed successfully, all knowledge-stage `done` flags stayed `False` on the current attack suites, Thinking kept every conversational field at `<= 0.6`, Purpose kept all done-gating fields below the lock threshold, and Goals kept `done=False` even in the explicit solo-revision case where `team_size` itself rose to `0.95`
- resolved later the same day: the serializer warning debt was paid by switching every structured-output boundary to `method="function_calling"`

### 2026-04-09 | Data-agent confidence lock audit

- changed: aligned `backend/data/prompts/job.py`, `backend/data/prompts/major.py`, and `backend/data/prompts/uni.py` with Python's shared `> 0.8` done threshold so `0.7-0.8` is explicitly provisional instead of lock-safe
- fixed: retrieval-stage extractor prompts now name Python as the done owner, reserve `> 0.8` for lock-safe fields only, and explicitly keep title, prestige, and bridge claims at or below `0.8` until they survive pressure
- verified:
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/job_attack.jsonl --graph job`
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/job_attack.jsonl --graph job_eval`
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/major_attack.jsonl --graph major`
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/major_attack.jsonl --graph major_eval`
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/uni_attack.jsonl --graph uni`
  - `venv\Scripts\python.exe eval/run_eval.py --mode multi --file eval/uni_attack.jsonl --graph uni_eval`
- found: all replay runs completed successfully; `job.done`, `major.done`, and `university.done` all stayed `False` on the current attack suites; `job` and `major` kept every done-driving field below `0.6`; `uni` kept `prestige_requirement` / `target_school` at or below `0.6` while only the explicitly categorical `campus_format` rose above `0.8`
- open: trace serialization still emits the same cross-cutting Pydantic serializer warnings, so warning cleanup remains the cheapest next hardening target

### 2026-04-09 | Structured-output serializer debt cleanup

- changed: forced every `ChatOpenAI.with_structured_output(...)` call in `backend/` onto `method="function_calling"` instead of relying on the current `langchain_openai` default `json_schema` path
- root cause: this installed `langchain_openai` build defaults `ChatOpenAI.with_structured_output(...)` to `method=\"json_schema\"`, which drives OpenAI parsed-response objects through `response.model_dump()` and triggers `PydanticSerializationUnexpectedValue` warnings on the embedded `parsed` field
- verified:
  - warning-as-error invoke on `thinking_graph`
  - warning-as-error invoke on `job_graph`
  - warning-as-error invoke on `uni_graph`
  - warning-as-error invoke on `orchestrator_graph.input_llm`
  - `venv\Scripts\python.exe -m unittest test_evaluation_graph_contract.py`
- found: the old warning path is gone on the audited graph boundaries; the debt was in LangChain/OpenAI structured-output method selection, not in `eval/run_eval.py`
- next: move up one hardening layer and decide between orchestrator replay and broader full-path audit

### 2026-04-09 | Anchor-stage docs residue cleanup

- changed: refreshed the active architecture, state, and output-prompt docs so they describe `anchor_stage`, `anchor_mode`, and `requested_anchor_stage` instead of the removed `forced_stage`, `rebound`, and `user_drill` model
- fixed: cleaned the last stale live counter-table line that still documented `rebound_count`, and aligned the docs with the current six-counter backend contract
- verified:
  - backend sweep for `user_drill`, `forced_stage`, `rebound`, `rebound_target`, and `rebound_count` returned no live runtime matches
  - active docs now show `anchor_stage` routing, `requested_anchor_stage`, `path_debate_ready`, and the Case A operative vs B1/B2 signal block split

### 2026-04-09 | Output-compiler mutual-exclusivity hardening

- changed: reworked `backend/data/prompts/output.py` so prompt injection follows audited lanes instead of stacking competing owners; Case A keeps one operative mode owner, while B1/B2 stay stage-owned and only receive additive context plus signal blocks
- changed: collapsed the old contradict + redirect prompt path into one `PIVOT_REDIRECT_SIGNAL_BLOCK`, so cross-stage pull is acknowledged and offered as an optional pivot without replacing the active-stage question
- changed: made `reality_gap` operative in B1/B2, kept `core_tension` additive in B1/B2, merged burnout + urgency into a single pacing context, and made stage blocks explicitly render the active `anchor_stage`
- reviewed: no new runtime break found in the audited compiler/routing changes; the only residue found was doc drift in the output-prompt architecture note, which was updated the same pass

### 2026-04-09 | Vault bootstrap and mirror-sync helper

- changed: added `scripts/pathfinder_ops.py` with two small workflow commands: `bootstrap` prints the mandatory vault read packet in order, and `sync-dev-log` wraps paired repo/vault day-file creation plus index rebuild/validation
- changed: added `test_pathfinder_ops.py` to lock the bootstrap packet order and the paired dev-log sync contract
- changed: updated `AGENTS.md` and `logs/README.md` so the helper is discoverable without weakening the vault-first rule
- verified:
  - `python -m py_compile scripts\pathfinder_ops.py`
  - `python -m unittest test_pathfinder_ops.py test_manage_dev_log.py test_manage_vault_log.py`

### 2026-04-09 | Escalation-family normalizer and Case C closing update

- changed: normalized Python escalation output in `backend/orchestrator_graph.py` so `escalation_reason` now carries `family`, `primary_pattern`, optional `supporting_patterns`, and preserved raw `details` instead of only raw joined counter strings
- changed later the same pass: sorted `supporting_patterns` by escalation-family priority so the normalized output stays deterministic across raw-reason order
- covered: the live escalation surface now maps cleanly across `troll_termination`, `disengagement_limit`, `avoidance_limit`, `vague_limit`, `contradict_count`, window-triggered troll/disengagement/avoidance/vague/contradict patterns, and compliance escalation
- changed: updated `backend/data/prompts/output.py` Case C to read the normalized reason first, then derive tone from `family` and wording from `primary_pattern`, including explicit handling for `boundary_violation`, `cannot_engage`, `active_resistance`, `instability_in_answers`, and `unknown`
- fixed: Case C no longer under-specifies vague/contradiction/window-driven session endings or depends on the old four-reason troll/disengagement/avoidance/compliance prompt contract
- documented: synced the active output-prompt architecture note with the live Case C override so the official prompt doc matches runtime behavior
- verified:
  - `python -m unittest test_orchestrator_graph_contract.py`
  - `python -m py_compile backend/orchestrator_graph.py backend/data/prompts/output.py`

### 2026-04-09 | Python-function-check workflow and grouped backend proof

- changed: added `python_function_check/HOW_TO_USE.md` plus four grouped report docs for shared stage-profile utils, orchestrator policy, stage-helper parity, and output/evaluation seams
- changed: added `test_stage_profile_utils_contract.py` and `test_stage_graph_helper_contract.py`, expanded `test_orchestrator_graph_contract.py` and `test_output_graph_contract.py`, and converted `test_thinking_graph_contract.py` / `test_uni_graph_contract.py` from pytest-style bare functions onto `unittest` so the documented commands actually execute those files
- fixed: `backend/thinking_graph.py::_apply_verification_caps()` now normalizes the Thinking profile after confidence caps, so single-turn capped fields no longer leave `done=True` below threshold
- verified:
  - `venv\Scripts\python.exe -m unittest test_stage_profile_utils_contract.py`
  - `venv\Scripts\python.exe -m unittest test_orchestrator_graph_contract.py`
  - `venv\Scripts\python.exe -m unittest test_stage_graph_helper_contract.py test_thinking_graph_contract.py test_uni_graph_contract.py`
  - `venv\Scripts\python.exe -m unittest test_evaluation_graph_contract.py test_output_graph_contract.py test_output_prompt_contract.py test_stage_contract.py test_main_contract.py`
- found: all four grouped commands now pass; Group 3 was the only bucket that exposed a real backend inconsistency, and the stale Thinking `done` flag bug is now closed

### 2026-04-09 | Python-function-check docs migrated into vault evaluation domain

- changed: moved the canonical Python-function-check workflow docs out of the repo-local `python_function_check/` folder and into the vault evaluation source-doc tree under `projects/pathfinder/sources/docs/evaluation/`
- changed: added matching note summaries under `projects/pathfinder/notes/` so the new workflow follows the existing evaluation-domain router pattern instead of living as repo-only markdown
- changed: updated the evaluation hub, PathFinder project README router, replay-evaluation workflow doc, live context, and repo README so the canonical location is unambiguous
- removed: deleted the repo-local markdown copies from `D:\ANHDUC\Path_finder\python_function_check\`
