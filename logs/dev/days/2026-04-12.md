# 2026-04-12

Summary: Thinking test double-submit bug fixed; live frontend user-like eval found and patched quiz-seed and Goals schema blockers

> **TL;DR**: `/test/{session_id}` now writes quiz state safely, live frontend eval exposed the missing seeded ThinkingProfile fields and missing nested Goals done flags, and the frontend live-eval workflow now has an optimized user-like run protocol.

## Updates

### 2026-04-12 | Thinking test checkpointer write fix

- fixed: `/test/{session_id}` previously worked on the first Thinking test submission but returned HTTP 500 on the second submission for the same session
- cause: LangGraph could no longer infer the update owner after the first checkpointer write and raised `InvalidUpdateError: Ambiguous update, specify as_node`
- changed: added `CHECKPOINT_PATCH_NODE = "__start__"` in `main.py`
- changed: `/test/{session_id}` and `/debug/state/{session_id}` now call `aupdate_state(..., as_node=CHECKPOINT_PATCH_NODE)`
- changed: added a regression test that posts `brain_type` then `riasec_top` to the same session and verifies the merged `thinking` state
- verified:
  - `venv\Scripts\python -m unittest test_debug_trace_contract.py test_main_contract.py`
  - `venv\Scripts\python -m py_compile main.py test_debug_trace_contract.py`
  - live HTTP smoke against `http://127.0.0.1:8000/test/<session_id>` with MI then RIASEC payloads

### 2026-04-12 | Live user-like frontend evaluation

- ran: real frontend flow with a mock grade-12 product/UX-leaning student profile
- ran: optimized UI test submission for both RIASEC and Brain Test
- ran: 24-turn traced chat from Thinking through Purpose and into the first Goals extraction
- trace: `eval/threads/f942d643-8941-40f7-ab6d-d378e0e6fe7f/traces/live_0001.json` through `live_0024.json`
- fixed: quiz-seeded Thinking state now fills required ThinkingProfile fields with low-confidence `not yet` entries before merging `brain_type` and `riasec_top`
- fixed: `GoalsLongProfile.done` and `GoalsShortProfile.done` now default to `False`, allowing Python normalization to recompute nested done flags when structured output omits them
- documented: canonical frontend workflow now includes batched quiz clicking, latest-response-only checks, Profile/Stage checks every 5 turns, and raw-state completion verification
- flaw recorded: assistant wording claimed Goals/Job/Major progress before raw backend state agreed
- flaw recorded: Purpose repeated the same risk tradeoff too many turns before completing
- verified:
  - `venv\Scripts\python -m unittest test_stage_profile_utils_contract.py test_debug_trace_contract.py test_main_contract.py`

### 2026-04-12 | Uncertainty attack pre-chat test bugs

- mission: attack with uncertainty using a normal student who does not have a stable path yet
- documented: added `eval/UNCERTAINTY_ATTACK_REPORT_2026-04-12.md` before starting the run
- found: a balanced/uncertain Brain Test can produce `brain_type: []`, which previously left the Brain Test card incomplete
- fixed: `/test/{session_id}` now treats provided empty lists as submitted tests via `request.model_fields_set`
- fixed: frontend `testStatus` now tracks MI/RIASEC completion separately from non-empty result values
- fixed: frontend state merge now keeps `testStatus` booleans sticky once true instead of letting a later test response reset the other flag
- paused: full uncertainty chat session deferred by user request; next run should start fresh from the same report
- verified:
  - `npm run lint`
  - `npm run build`
  - `venv\Scripts\python -m unittest test_debug_trace_contract.py test_stage_profile_utils_contract.py test_main_contract.py`
